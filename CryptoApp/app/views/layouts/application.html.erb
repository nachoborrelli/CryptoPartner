<!DOCTYPE html>
<html>
  <head>
    <title>CryptoApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <% if user_signed_in? %>
      <li class="nav-item">
        <%= link_to "#{current_user.username}", edit_user_registration_path,:class=>"nav-link active" %>
      </li>
      <li class="nav-item">
        <%= link_to "Log Out", destroy_user_session_path, method: :delete,:class=>"nav-link active" %>
      </li>
    <% else %>
      <li class="nav-item">
        <%= link_to "Log In", new_user_session_path ,:class=>"nav-link active"%>
      </li>
      <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
